# SSH host for production server (e.g., user@server.com or an SSH config alias)
PROD_SSH_HOST=

# Absolute path to the application directory on the production server
PROD_SERVER_APP_DIR=application-patient-care
